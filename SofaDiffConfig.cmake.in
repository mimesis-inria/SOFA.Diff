# CMake package configuration file for the plugin @PROJECT_NAME@

@PACKAGE_GUARD@
@PACKAGE_INIT@

#set(SOFADIFF_HAVE_SOFA_GUI_QT @SOFADIFF_HAVE_SOFA_GUI_QT@)
#set(SOFADIFF_HAVE_QT5_CORE @SOFADIFF_HAVE_QT5_CORE@)

#find_package(Sofa.Component.Visual QUIET REQUIRED)

#if(SOFADIFF_HAVE_SOFA_GUI_QT)
#    find_package(Sofa.GUI.Qt QUIET REQUIRED)
#endif()
#if(SOFADIFF_HAVE_QT5_CORE)
#    find_package(Qt5 COMPONENTS Core QUIET REQUIRED)
#endif()

if(NOT TARGET @PROJECT_NAME@)
    include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@Targets.cmake")
endif()

check_required_components(@PROJECT_NAME@)